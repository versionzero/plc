
begin
   const num = 10;  $ this is a comment!
   integer x, y;    $ this is another comment!
   Boolean b;
   proc max
   begin
      if x > y -> write x. [] ~(x > y) -> write y.
      fi
   end
end.
